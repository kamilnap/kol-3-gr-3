{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Bio list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Inta</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_bio_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>
                    <a href="{{ path('admin_bio_show', { 'id': entity.id }) }}">
                    {{ entity.name }}
                    </a>
                </td>
                <td>{{ entity.inta }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_bio_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_bio_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_bio_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
